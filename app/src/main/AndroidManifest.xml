<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rex.project.helep">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Helep">
        <activity
            android:name=".view.activities.addTask.AddTask"
            android:exported="true" />
        <activity
            android:name=".view.activities.home.HomeActivity"
            android:exported="true" />
        <activity
            android:name=".view.activities.register.RegisterActivity"
            android:exported="true" />
        <activity
            android:name=".view.activities.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>